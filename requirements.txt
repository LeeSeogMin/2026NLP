# =============================================================
# 딥러닝 자연어처리 — 통합 실습 환경
# LLM 시대의 NLP 엔지니어링: 원리부터 배포까지
# =============================================================
# 설치: pip install -r requirements.txt
# GPU 버전 PyTorch: https://pytorch.org/get-started/locally/
# =============================================================

# ----- Core (ch1-4: PyTorch 기초 ~ Transformer) -----
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.13.0
scikit-learn>=1.3.0
tqdm>=4.65.0
pandas>=2.1.0

# ----- NLP 기초 (ch3: 임베딩) -----
gensim>=4.3.0
nltk>=3.8.0

# ----- Hugging Face 생태계 (ch5: BERT/GPT, ch9-10: 파인튜닝) -----
transformers>=4.36.0
tokenizers>=0.15.0
datasets>=2.14.0
evaluate>=0.4.0
accelerate>=0.25.0
sentence-transformers>=2.2.0
peft>=0.7.0

# ----- LLM API (ch6: 프롬프트 엔지니어링) -----
openai>=1.12.0
anthropic>=0.18.0

# ----- 토픽 모델링 (ch8) -----
bertopic>=0.16.0
umap-learn>=0.5.0
hdbscan>=0.8.33

# ----- RAG (ch11) -----
langchain>=0.1.0
langchain-community>=0.0.10
langchain-huggingface>=0.0.1
faiss-cpu>=1.7.4
chromadb>=0.4.0

# ----- AI Agent (ch12) -----
langgraph>=0.0.26

# ----- 배포 (ch13) -----
fastapi>=0.109.0
uvicorn>=0.27.0
gradio>=4.0.0

# ----- 시각화 (보조) -----
plotly>=5.0.0

# ----- 개발 도구 -----
python-dotenv>=1.0.0
jupyter>=1.0.0
ipykernel>=6.26.0

# ----- 프로젝트 도구 (Notion 발행, Multi-LLM 리뷰) -----
notion-client>=2.2.0
requests>=2.31.0

# ----- 선택 설치 (GPU 양자화, Linux/CUDA 전용) -----
# bitsandbytes>=0.41.0    # ch10 QLoRA — pip install bitsandbytes
